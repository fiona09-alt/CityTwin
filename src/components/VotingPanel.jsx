import{priorities,projects}from'../data/projects';export default function VotingPanel({activeProject,selectedPriority,onPriorityChange,voteSubmitting,voteSubmitted,voteError,onSubmit}){let p=projects.find(x=>x.id===activeProject);return <section className="card voting"><div className="kicker">Citizen priorities</div><h2>WHAT MATTERS MOST TO YOU?</h2><div className="priority">{priorities.map(x=><button className={selectedPriority===x.id?'active':''} onClick={()=>onPriorityChange(x.id)} key={x.id}>{x.label}</button>)}</div><p>Voting for <b style={{color:p.color}}>{p.name}</b></p><button className="submit" disabled={!selectedPriority||voteSubmitting} onClick={onSubmit}>{voteSubmitting?'Submitting vote...':'Submit vote'}</button>{voteSubmitted&&<em className="success">Vote submitted!</em>}{voteError&&<em className="error">{voteError}</em>}</section>}
