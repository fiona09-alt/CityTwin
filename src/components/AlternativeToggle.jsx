import{projects}from'../data/projects'
export default function AlternativeToggle({activeProject,onProjectChange,showProject,onToggleProject}){return <><div className="kicker">Explore alternatives</div><div className="toggles">{projects.map((p,i)=><button key={p.id} className={activeProject===p.id?'active':''} style={{'--c':p.color}} onClick={()=>onProjectChange(p.id)}><small>{'ABC'[i]}</small>{p.name}</button>)}</div><div className="before-after"><button className={!showProject?'selected':''} onClick={()=>onToggleProject(false)}>BEFORE</button><button className={showProject?'selected':''} onClick={()=>onToggleProject(true)}>AFTER</button></div></>}
